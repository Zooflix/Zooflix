server {
    listen 80;
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html = 404;
    index index.html index.htm;
  }

  location /api/ {
    proxy_pass https://api.zooflix.duckdns.org/;
  }
}